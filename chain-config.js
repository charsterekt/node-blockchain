// Configurable values and params for various aspects of the blockchain

const TRANSACTION_FEE = 1
const INITIAL_BALANCE = 100
// The number of transactions that can be added in a single block
const TRANSACTION_THRESHOLD = 5

module.exports = {
    TRANSACTION_FEE,
    INITIAL_BALANCE,
    TRANSACTION_THRESHOLD
}